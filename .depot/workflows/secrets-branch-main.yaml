name: "Secrets v2: Branch main"

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  verify:
    runs-on: depot-ubuntu-22.04
    steps:
      - name: Print secrets context
        env:
          TEST_SECRET: ${{ secrets.TEST_SECRET }}
          COMBO_SECRET: ${{ secrets.COMBO_SECRET }}
          TEST_VAR: ${{ vars.TEST_VAR }}
        run: |
          echo "=== Secrets v2: Branch main test ==="
          echo "Branch: $GITHUB_REF_NAME"
          echo ""
          echo "TEST_SECRET length: ${#TEST_SECRET}"
          if [ -z "$TEST_SECRET" ]; then
            echo "❌ TEST_SECRET is empty"
          else
            echo "✅ TEST_SECRET is set (masked)"
          fi
          echo ""
          echo "COMBO_SECRET length: ${#COMBO_SECRET}"
          if [ -z "$COMBO_SECRET" ]; then
            echo "ℹ️  COMBO_SECRET is empty (expected — no environment set)"
          else
            echo "⚠️  COMBO_SECRET is set — should be empty without environment"
          fi
          echo ""
          echo "TEST_VAR value: '$TEST_VAR'"
